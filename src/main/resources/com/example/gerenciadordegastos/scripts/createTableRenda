CREATE TABLE RENDA (
    id integer not null,
    id_usuario integer not null,
    titulo varchar(200),
    valor double precision,
    data date,
    descricao varchar(255),
    dta_add timestamp,
    dta_alt timestamp
);